#pragma once

#include <vector>
#include "trixel.hpp"

namespace ICoDF_HTM
{
  typedef struct Octahedron_s
  {
    std::string _name;
    trixel_t* _rootTrixels[8]; // [0] = N0, [4] = S0
  } Octahedron_t;
}
